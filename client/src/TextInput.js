import React from "react";
import "./TextInput.css";
import SectionHeader from "./SectionHeader";

const TextInput = () => {
  return (
    <section id="text-input">
      <SectionHeader title={"Enter Your Message"} />
    </section>
  );
};

export default TextInput;
